go 1.26.6

// The repository is a set of modules that are developed and released together.
// The workspace is what makes a change to the core visible to every adapter
// without a tagged release in between.
use (
	.
	./aws
	./database/memcached
	./database/mongodb
	./database/redis
	./database/sql
	./database/sqlc
	./example
	./http
	./messaging
	./preset/full
)
